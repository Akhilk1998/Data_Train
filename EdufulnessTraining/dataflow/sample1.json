{
	"name": "sample1",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "ds_sample",
						"type": "DatasetReference"
					},
					"name": "Studentdata"
				},
				{
					"dataset": {
						"referenceName": "ds_sample",
						"type": "DatasetReference"
					},
					"name": "source1"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "ds_destination",
						"type": "DatasetReference"
					},
					"name": "sink1"
				},
				{
					"dataset": {
						"referenceName": "ds_destination",
						"type": "DatasetReference"
					},
					"name": "sink2"
				}
			],
			"transformations": [
				{
					"name": "select1"
				},
				{
					"name": "totalmark"
				},
				{
					"name": "TopRank"
				},
				{
					"name": "filtemark"
				},
				{
					"name": "sort1"
				},
				{
					"name": "split1"
				},
				{
					"name": "surrogateKey1"
				}
			],
			"scriptLines": [
				"source(output(",
				"          {Roll NO} as string,",
				"          Name as string,",
				"          class as string,",
				"          mark as string",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     ignoreNoFilesFound: false) ~> Studentdata",
				"source(output(",
				"          {Roll NO} as string,",
				"          Name as string,",
				"          class as string,",
				"          mark as integer",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     ignoreNoFilesFound: false) ~> source1",
				"Studentdata select(mapColumn(",
				"          {Roll No} = {Roll NO},",
				"          Name,",
				"          Mark = mark,",
				"          Class = class",
				"     ),",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true) ~> select1",
				"source1 derive(percentage = (mark/100)*100) ~> totalmark",
				"surrogateKey1 rank(asc(mark, true),",
				"     output(Mark as long),",
				"     dense: true) ~> TopRank",
				"TopRank filter(TopRank@Mark>=90) ~> filtemark",
				"filtemark sort(asc({Roll NO}, true),",
				"     partitionLevel: true) ~> sort1",
				"totalmark split(mark<90,",
				"     mark>90,",
				"     mark==90,",
				"     disjoint: true) ~> split1@(markless90, markgreater90, mark90)",
				"split1@markless90 keyGenerate(output(rollno as long),",
				"     startAt: 1L,",
				"     stepValue: 1L) ~> surrogateKey1",
				"select1 sink(allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     partitionFileNames:['selectout'],",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true,",
				"     partitionBy('hash', 1)) ~> sink1",
				"sort1 sink(allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     partitionFileNames:['filtersortout'],",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true,",
				"     partitionBy('hash', 1)) ~> sink2"
			]
		}
	}
}